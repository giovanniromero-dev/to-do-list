# To Do List

Vanilla JavaScript task list app with a simple browser UI.

## Overview

This project lets users add tasks, mark them as completed, reorder the list, and view the current date in the interface.

## Features

- create new tasks
- toggle task completion
- reorder task rendering
- display the current date

## Usage

Open `index.html` in a browser and interact with the list locally.
